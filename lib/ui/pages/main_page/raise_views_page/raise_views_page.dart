import 'package:flutter/material.dart';

class RaiseViews extends StatelessWidget {
  const RaiseViews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: getBody(),
    );
  }

  Widget getBody() => Container();
  Widget levelCard() => Container();
}

